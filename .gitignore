*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

### Java ###
# Compiled class file -> 컴파일러에 의해 생성된 자바 클래스의 바이너리 파일.
*.class

# Log file -> 말 그대로 로그 파일
*.log

# BlueJ files -> 자바를 사용하는 BlueJ라는 소프트웨어에서 사용하는 것
*.ctxt

# Mobile Tools for Java (J2ME)
.mtj.tmp/

# Package Files # -> 압축되었거나, 패키징 되어 있는 파일들. 컴파일하면 생성되므로 굳이 올릴 필요 없음.
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar

# virtual machine crash logs, see http://www.java.com/en/download/help/error_hotspot.xml
hs_err_pid*
replay_pid*

### AndroidStudio ###
# Covers files to be ignored for android development using Android Studio.

# Built application files -> 실제로 실행하기 위해 만들어진 파일들.
*.apk
*.ap_
*.aab

# Files for the ART/Dalvik VM -> Dalvik 실행 파일. Android용으로 작성된 컴파일 코드가 들어있음.
*.dex

# Java class files

# Generated files -> 용량이 크고, 컴파일시 생성이 되는 위치이므로 굳이 넣을 필요 없음.
bin/
gen/
out/

# Gradle files -> Gradle 이라고 하는 빌드 도구에서 사용하는 데이터.
.gradle
.gradle/
build/

# Signing files
.signing/

# Local configuration file (sdk path, etc) -> 말 그대로 로컬에서의 설정 파일. 로컬 환경마다 다르므로 굳이 올릴 필요 없음.
local.properties

# Proguard folder generated by Eclipse 
proguard/

# Log Files

# Android Studio -> 이 부분 또한 용량이 크고, 컴파일 등을 할 경우 생성이 되는 파일들이므로 굳이 올릴 필요 없음.
/*/build/
/*/local.properties
/*/out
/*/*/build
/*/*/production
captures/
.navigation/
*.ipr
*~
*.swp

# Keystore files
*.jks
*.keystore

# Google Services (e.g. APIs or Firebase)
# google-services.json

# Android Patch
gen-external-apklibs

# External native build folder generated in Android Studio 2.2 and later
.externalNativeBuild

# NDK -> Android NDK에서 사용하는 부분. C나 C++을 사용하게 해주나, Java로 개발하므로 의미 없음.
obj/

# IntelliJ IDEA -> IntelliJ를 위한 파일. 코딩 편의를 위한 데이터 이므로 굳이 올릴 필요 없음
*.iml
*.iws
/out/

# User-specific configurations -> 작성하는 사람 개개인 마다 달라지는 데이터들. 올릴 필요 없음.
.idea/caches/
.idea/libraries/
.idea/shelf/
.idea/workspace.xml
.idea/tasks.xml
.idea/.name
.idea/compiler.xml
.idea/copyright/profiles_settings.xml
.idea/encodings.xml
.idea/misc.xml
.idea/modules.xml
.idea/scopes/scope_settings.xml
.idea/dictionaries
.idea/vcs.xml
.idea/jsLibraryMappings.xml
.idea/datasources.xml
.idea/dataSources.ids
.idea/sqlDataSources.xml
.idea/dynamic.xml
.idea/uiDesigner.xml
.idea/assetWizardSettings.xml
.idea/gradle.xml
.idea/jarRepositories.xml
.idea/navEditor.xml

# Legacy Eclipse project files -> Eclipse 프로젝트에서 사용하던 파일들.
.classpath
.project
.cproject
.settings/

# 아래 부분은 플러그인 관련 파일임.

# mpeltonen/sbt-idea plugin -> JetBrains에서 사용하는 sbt에 대한 파일.
.idea_modules/

# JIRA plugin -> 백 트래킹이나 프로젝트 관리에 사용하는 JIRA 소프트 웨어 관련 파일.
atlassian-ide-plugin.xml

# Mongo Explorer plugin -> 데이터베이스 관리 플러그인.
.idea/mongoSettings.xml

# Crashlytics plugin (for Android Studio and IntelliJ) -> 앱 오류를 파악하게 해주는 프로그램 Crashlytics에 대한 파일.
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

### AndroidStudio Patch ###

!/gradle/wrapper/gradle-wrapper.jar